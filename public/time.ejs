<div class="pagebg timer"></div>
<div class="container">
  <h1 class="t_nav">
    <span>时光飞逝，机会就在我们眼前，何时找到了灵感，就要把握机遇，我们需要智慧和勇气去把握机会。</span>
    <a href="/" class="n1">首页</a>
    <a href="/" class="n2">时间轴</a>
  </h1>
  <div class="timebox">
    <%
      function getDate (date) {
        let d = new Date(date)
        return [d.getFullYear(), d.getMonth() + 1, d.getDate()].join('-')
      };
    %>
    <ul id="list">
      <% articles.forEach(item => { %>
        <li><span><%- getDate(item.created_at) %></span><a title="<%- item.title %>"><%- item.title %></a></li>
      <% }) %>
    </ul>
  </div>
</div>